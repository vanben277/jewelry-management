spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jewelry_management
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql:
logging:
  level:
    org:
      hibernate:
        type: trace